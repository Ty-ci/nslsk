[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: let React Router handle client-side routes so that
# deep links and page refreshes don't 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
