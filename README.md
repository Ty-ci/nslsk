# nslsk

A simple static single-page website built with **Vite + React + TypeScript + Tailwind CSS**. The
nav menu anchors to scrollable sections of one page (no client-side routing).

## Getting started

```bash
npm install      # first time only
npm run dev      # start the dev server at http://localhost:5173
```
